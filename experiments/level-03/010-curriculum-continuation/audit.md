# Independent audit of the curriculum continuation

**All four development and all four frozen confirmation conditions pass every
unchanged gate.** This completes the assisted one-blank behavioral milestone
under the declared interface and whole Step 2→Step 3 control question.
Experiment 007 remains the failed test of whether correct additional Step 3
feedback was necessary. No neural simulations were run for this audit.

Reviewed source `b165e38`, final development records, all sixteen snapshots,
each arm's original Step 2 source memory, and anatomical edge identities.
Independently reconstructed all metrics, control event schedules, memory
statistics, and erasure comparisons. Development results were committed at
`1bfb1e6` before reserved-family confirmation began.

| Source seed | Mapping | Paired balanced accuracy / completion | Frozen / no-feedback / inconsistent balanced accuracy | Minimum target margin beyond deadband |
|---|---:|---:|---:|---:|
| 20260919 | 0 | 100% / 100% | 0% / 0% / 12.50% | +0.375 Hz |
| 20260919 | 1 | 100% / 100% | 0% / 0% / 16.67% | +0.125 Hz |
| 20260920 | 0 | 100% / 100% | 0% / 0% / 0% | +0.375 Hz |
| 20260920 | 1 | 100% / 100% | 0% / 0% / 4.17% | +0.375 Hz |

Every paired agent also has 100% raw accuracy, accept/reject recall, acceptance
precision, and balanced accuracy in every candidate, blank-position, and view
subgroup. No paired judgment times out. Increasing and decreasing first-accept
scans both complete every board. All curriculum controls have zero completion.
Every paired-versus-control advantage exceeds the fixed 25 percentage-point
requirement. Recomputed values agree with the saved gates.

## Exact replication and the control question

All **768 paired training records** exactly reproduce 007, including decisions,
feedback events, full spike hashes, and every effective/latent memory summary.
All four paired final W/u/w arrays and all **64 paired frozen recall records**
also reproduce 007 exactly. The successful training trajectory was not modified
to obtain this control result.

The changed intervention is each control's initial history. In 007 every arm
received the already learned paired Step 2 state; its inconsistent controls
then achieved 87.50%, 83.33%, 91.67%, and 87.50% balanced accuracy. Here each arm
continues its own original paired, frozen, no-feedback, or inconsistent source
state. This tests the whole learning history, not the marginal necessity of
correct new board-labelled pairing. Both results must remain visible; the
present pass does not overturn 007's failed incremental-learning claim.

## Development coverage and physical measurements

The manifest covers all 96 development grids, sixteen blank positions, four
candidates, and three views: 18,432 judgments comprising 4,608 accepts and
13,824 rejects. This represents 4,608 rendered boards, or 1,536 distinct masked
grids across three views. Every label agrees with the unique missing digit;
each KC input hash reproduces its sorted graph indices.

Those presentations collapse to sixteen neural representations, each aliased
1,152 times. They are the same sixteen representations used in conditioning.
There are 1,040 physical frozen evaluations: sixteen untrained baseline and
256 each for own-source inherited state, final recall, stage erasure, and full
erasure. The four paired endpoints contain 64 actual decisions, not 73,728
independent neural tests.

All 500 ms trace sums reproduce the recorded MBON rates and fixed
`MBON11 - MBON07 - 1.625 Hz` score. Thresholding at +/-2 Hz, followed by the
declared semantic mapping, reproduces every action. All onset times are zero;
each evaluation records 168 total KC spikes from the 24 selected cells,
zero DAN spikes, and unchanged W/u/w. No decoder, current, input, or numerical
gate changes accompany the control-history change.

## Teaching, memory, and erasure

Checked all 3,072 pre-feedback judgments and 360 subsequent teaching phases.
Each arm has eight fixed 24-trial epochs, with each accept input repeated
three times and each reject input once per epoch. Independently regenerated
cue orders match. Every paired teaching event follows a wrong/undecided action
and uses the correct mapped pulse. Paired event counts are 5, 18, 5, and 17;
all occur in the first two epochs, and all concern invalid candidates.

Frozen/no-feedback controls replay the paired schedule exactly. Inconsistent
controls match the declared shuffle of all events, including no-teaching
entries. They preserve total event and pulse counts while moving active
windows; 4/5, 7/18, 3/5, and 11/17 pulses contradict their input's target.
This is not per-input balanced feedback or an active-window-matched control.

Every decision is frozen before feedback. A teaching event comprises 200 ms
target DAN stimulation and 250 ms passive relaxation. Whole-KC logs record
zero KC spikes during both phases; the retained cue trace supplies the local
depression term. Reward pulses produce 270 PAM11 spikes; aversive pulses
produce 36 PPL101 spikes, with zero activity in the other compartment.
No-feedback DAN counts are zero. Phase/trial memory continuity and all frozen
phase invariants pass.

Each arm's initial W/u/w equals its own original source. Frozen and no-feedback
endpoints remain at their original zero-memory baselines. After accounting
for analytic passive decay of each arm's own source, added paired weakening
occupies 132, 256, 135, and 256 stimulated edges; inconsistent weakening occupies
123, 256, 133, and 240. All other latent entries follow passive decay within
2e-12. Reporting 909 changed edges in a learned arm includes previously
learned but unstimulated edges that merely decay; it is not a count of newly
acquired associations.

Snapshot hashes and memory statistics agree with the records, and plastic
indices reproduce exactly the original 7,835 anatomical KC-to-MBON07/11 edges.
All 256 stage-erasure comparisons restore each arm's own source W/u/w,
full spike hashes, traces, rates, and actions exactly. All 256 full-erasure
comparisons restore the untrained baseline. Nonplastic preservation additionally
relies on the runner's full-weight hash assertion after resetting only plastic
memory; no full post-training network weight snapshot is independently saved.

## Frozen reserved-family confirmation

The held-out manifest covers all 192 reserved grids, sixteen blank positions,
four candidates, and three views: **36,864 candidate presentations** and
**9,216 rendered boards** (3,072 distinct masked grids across three views).
Every paired state scores 100% on all judgment, class, candidate, position,
view, precision, and completion metrics, with no timeouts. Control metrics
and margins equal the development table. The complete numerical gate passes
separately for every state.

An independent enumeration from row/column/box constraints reproduces all
288 complete grids. A separate closure calculation using Sudoku symmetry
generators reproduces the 96/192 structural families. Indexing every possible
one-blank mask yields exactly 4,608 distinct, uniquely completed masked boards.
Development and confirmation grid-ID sets are disjoint and together cover
the complete universe. Their full rendered image hashes are also disjoint;
every one of the combined 55,296 presentation hashes is distinct.

The sixteen KC input hashes, however, are **identical across the two families**.
Each confirmation representation has 2,304 aliases. All 1,040 physical
confirmation records exactly reproduce their development counterparts when
matched by source state, arm, phase, and input: full spike hashes, traces,
rates, actions, and W/u/w. This is successful board/rendering coverage using
familiar neural representations, not a neural generalization test. Across
development and confirmation the paired endpoints contribute 128 physical
recall measurements of sixteen inputs in four trained states.

Confirmed zero training and calibration epochs, curriculum control history,
the bound development-summary digest, and unchanged source/decoder/current/
input parameters. Every listed artifact hash and current source hash verifies.
Confirmation writes no training log or new memory snapshots. All inference
remains frozen; its 256 own-source and 256 full-erasure comparisons pass just
as in development. Results therefore depend on the states committed before
the reserved images were presented, without retraining or tuning from those
confirmation outcomes.

## Bounds and scope

Clipping exactly reproduces 007. Mapping 0 has none at recorded phase endpoints.
Mapping 1 reaches the latent lower clamp at ten and nine endpoints and effective
lower bounds at six and five endpoints, with up to sixteen effective weights
simultaneously at the lower bound. Each final mapping-1 snapshot has 64 weights
below 0.11 efficacy and minimum 0.10010016, despite zero final strict at-bound
counts. Phase logs cannot exclude additional within-phase clipping. The narrowest
successful margin is only +0.125 Hz; these deterministic results do not establish
robustness to perturbations or physiological uncertainty.

The fixed template parser, target-row attention, and sensory pooling remain
engineered. The row alone suffices for valid one-blank boards. This assay does
not establish learned visual recognition, independent row/column/box reasoning,
a newly learned Sudoku rule, general puzzle solving, or novel neural
generalization. The reserved board family aliases the same sixteen neural
representations. The passing frozen confirmation establishes complete
board/rendering coverage under this assisted interface.
